<UserControl x:Class="CombasLauncherApp.UI.Pages.BuildManagerPage.BuildManagerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CombasLauncherApp.UI.Pages.BuildManagerPage"
             xmlns:models="clr-namespace:CombasLauncherApp.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance local:BuildManagerPageViewModel}">
    <UserControl.Resources>
        <!-- Child template for BuildEntry -->
        <DataTemplate DataType="{x:Type models:BuildEntry}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}" Width="140" Margin="0,0,5,0"/>
                <TextBlock Grid.Column="1" Text="{Binding BuildDate}" />
            </Grid>
        </DataTemplate>
        <!-- Parent template for BuildCollection -->
        <HierarchicalDataTemplate DataType="{x:Type local:BuildCollection}" ItemsSource="{Binding Builds}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}" Margin="0,0,5,0"/>
                <TextBlock Grid.Column="1" Text="{Binding Builds.Count, StringFormat={}{0} / 50}" />
                <Button Grid.Column="2" Content="{StaticResource LOC_Delete}"
                        Command="{Binding  RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}, Path=DataContext.DeleteCollectionCommand}"
                        CommandParameter="{Binding}"/>
           </Grid>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>

            </Grid.RowDefinitions>
            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical" Margin="10" Background="{StaticResource OverlayBackground}">
                <TextBlock Text="Build Collections" FontWeight="Bold" FontSize="16" Margin="10,2"/>
                <ItemsControl ItemsSource="{Binding BuildCollections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander Header="{Binding Name}" IsExpanded="False" Margin="0,0,0,5">
                                <ItemsControl ItemsSource="{Binding Builds}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" Width="140" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding BuildDate}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Margin="10">
                <TextBox x:Name="CreateCollectionNameBox" Width="200" Margin="0,0,10,0" />
                <Button Content="{StaticResource LOC_Create}"
                        Command="{Binding CreateCollectionCommand}"
                        CommandParameter="{Binding Text, ElementName=CreateCollectionNameBox}" />
             
            </StackPanel>
            <Image Grid.Row="0" Grid.Column="1" Source="{Binding SelectedCollectionBuild.Image, FallbackValue='Resources/OopsImage.png'}" />
                    
        </Grid>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>

            </Grid.RowDefinitions>
            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical" Margin="10" Background="{StaticResource OverlayBackground}">
                <TextBlock Text="Loaded Builds" FontWeight="Bold" FontSize="16" Margin="10,2"/>
                <TextBlock Text="{Binding CurrentLoadedBuilds.Count, StringFormat={}{0} / 50}" Margin="10,2"/>

                <ListView ItemsSource="{Binding CurrentLoadedBuilds}"
                          SelectedItem="{Binding SelectedLoadedBuild}"
                          DisplayMemberPath="Name" Margin="10">
                    <ListView.View>
                        <GridView AllowsColumnReorder="False" >
                            <GridViewColumn Header="{StaticResource LOC_Name}" DisplayMemberBinding="{Binding Name}" Width="140"/>
                            <GridViewColumn Header="{StaticResource LOC_Date}" DisplayMemberBinding="{Binding BuildDate}" Width="200"/>
                        </GridView>
                    </ListView.View>
                </ListView>


            </StackPanel>
            <Image Grid.Row="0" Grid.Column="1" Source="{Binding SelectedLoadedBuild.Image, FallbackValue='Resources/OopsImage.png'}" />

        </Grid>
    </Grid>
</UserControl>
